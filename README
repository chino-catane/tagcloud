Mac
---
1. press [command + space bar] to open a search bar and type "terminal"
2. install pip with command : sudo easy_install pip
3. install Flask with command: sudo pip install Flask
4. install Python Image Library with command: sudo pip install pillow
